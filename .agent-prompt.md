Context: Repo=C:\Users\MLTZ\autoagent Branch=feat-add-config-file Worktree=C:\Users\MLTZ\.clawdbot\worktrees\feat-add-config-file

TASK (implement this first, do not explain DoD to me):
Add a simple JSON config loader to the project. Create a config/default.json file with a greeting template string. Create a lib/config.js module that reads config/default.json and allows overriding with config/local.json (gitignored). Update index.js to use the greeting template from config instead of hardcoded 'Hello, {name}!'. Add a test in tests/config.test.js verifying the config loader works and falls back correctly.

Definition of Done:
- PR created with gh pr create --fill
- Branch synced to main (no merge conflicts)
- CI passing (lint, types, unit tests)
- Include screenshots if UI changed
- Do NOT merge; stop and wait for human review

